# Session Handoff — 2026-04-10

## Completed

- Phase 1: `StateStore` now tracks symbol touch times, evicts stale symbol-scoped state conservatively, and exposes cleanup diagnostics. `ContextService` can prune its symbol-local memoization after eviction.
- Phase 2: REST fallback now skips fresh symbols, narrows requests to stale symbols, and uses `fetchTickers(...)` batching when multiple symbols need refresh.
- Phase 3: `src/engines/backtestEngine.ts` is no longer a scaffold. It is now a modern adapter boundary over the preserved legacy backtest modules.
- Phase 4: `ContextBuilder` hot-path allocations were reduced by replacing several repeated `map` / `slice` remaps with local loops. `RegimeDetector` did not need a new patch because the earlier slice-heavy issue is already gone in the current code.
- Phase 5: trade-side typing and directional PnL helpers are now side-ready for future short work without enabling short entries or changing current paper-long behavior.
- Follow-up telemetry pass: the older `TradingBot` max-drawdown manual-resume regression is fixed. Manual-resume-required drawdown pauses now emit explicit `RISK_CHANGE` metadata even in verbose mode.
- Follow-up diagnostics pass: `SystemServer` warmup remaining now uses the configured `architectWarmupMs` instead of a stale `30_000ms` assumption.
- Follow-up diagnostics pass: `/api/system` and `/api/bots` now expose drawdown-pause/manual-resume state separately from the shared portfolio kill switch.
- Follow-up UI pass: `SystemServer` now serves a separate compact monitor at `/compact`, with optional explicit auto-open through `AUTO_OPEN_COMPACT_UI=true` or `COMPACT_UI=true`.
- Follow-up consistency pass: entry now blocks during pending Architect challenger hysteresis with `architect_challenger_pending`.
- Follow-up consistency pass: managed-recovery invalidation now requires grace/confirmation for early regime-sensitive invalidation, and confirmed recovery target beats invalidation.
- Follow-up economics pass: `rsiReversion` now uses `minExpectedNetEdgePct: 0.0015` plus `maxTargetDistancePctForShortHorizon: 0.01`; entry diagnostics include `targetDistancePct`.
- Follow-up MTF pass: optional MTF frame snapshots, aggregation, and publish diagnostics now use internal horizon frame ids (`short`, `medium`, `long`) while leaving raw timeframe mapping in frame config / aggregation plumbing.
- Follow-up RSI MTF pass: `mtfParamResolver` resolves RSI entry hints/cap diagnostics only. Disabled/absent/unclear/unstable/insufficient/non-range MTF keeps the baseline cap; coherent range `medium` widens to `1.5x`, coherent range `long` widens to `2.0x`, and `short` remains baseline.
- Follow-up telemetry pass: full and compact entry telemetry now exposes published MTF diagnostics plus entry-side RSI cap-resolution fields (`resolvedMtfDominantFrame`, `resolvedMtfAdjustmentApplied`, resolved cap, target-distance profile, fallback/coherence reason).
- Follow-up runtime cleanup pass: `SystemServer` now exposes explicit `POST /api/bots/:botId/resume` for `max_drawdown_reached` bot pauses, guarded by portfolio kill-switch state; `bot.js` suppresses the Node strip-types experimental warning in the normal production bootstrap path; `OpenAttemptCoordinator` now rejects unsupported shorts explicitly instead of falling back to long.

## Still Open

- Full modern backtest integration is not done. The repo now has a stable adapter boundary, but replay still runs through legacy logic rather than the modern `ContextService` / `ArchitectService` / `TradingBot` path.
- Launcher preparation is still open:
  - startup needs an explicit `Normal` / `Debug` mode selector
  - `Debug` mode should drive a second configuration window for run-capture selection
  - the selected capture set should map cleanly onto `jsonl` output without baking debug concerns into trading logic
- Debug-output schema is still open:
  - decide which records are append-only event lines
  - decide which values are rolling counters or latest snapshots that should update by number instead of exploding log volume
- Short support is no longer just prep work, but it still needs an end-to-end audit across replay, analytics, and operator flows before it should be treated as fully settled.
- Pulse startup auto-open still depends on the default OS/browser opener; window sizing and browser-specific launch behavior remain intentionally non-guaranteed.
- MTF is enabled by default in `src/data/bots.config.json`; use `MTF_ENABLED=false` to disable it at runtime or `MTF_ENABLED=true` to force it on if JSON config is changed.
- MTF diagnostics are observable, but there is no UI control surface for MTF tuning.

## Update - 2026-04-21

- First-class short support has since landed across strategy decisions, entry economics, execution, exit handling, telemetry, chart markers, and tests.
- `ExecutionEngine` is side-aware for open/close accounting and exposes explicit short open/cover semantics while remaining paper-only.
- `OpenAttemptCoordinator` no longer falls back from short to long; unsupported short execution rejects explicitly.
- The old separate compact monitor is gone as a distinct UI surface. The active operator UI is now Pulse at `/`, backed by `/api/pulse` and `/api/pulse/stream`.
- `SystemServer` still supports explicit startup auto-open, but `/compact` now normalizes to `/` instead of serving a second UI architecture.
- Manual resume is no longer API-only in practice; Pulse exposes a focused resume action for `max_drawdown_reached` pauses.
- Additional trimming patches reduced wrapper noise in `TradingBot` and surrounding runtime code without breaking the current test suite.
- As of `2026-04-21`, `npm test` passes on the repository state at `HEAD`.

## Update - 2026-04-22

- First-wave audit fixes have now landed without redesigning the runtime:
  - portfolio kill-switch mode validation uses a single shared source of truth and rejects unsupported values explicitly
  - `StateStore.registerBot(...)` no longer restores stale `stopped` state for enabled bots
  - valid paused states are preserved across re-registration when they already carry a real `pausedReason`
- Exit semantics for `rsiReversion` are now capability-driven instead of strategy-name-driven:
  - `exitPolicy.qualification.rsiThresholdExit`
  - `exitPolicy.recovery.priceTargetExit`
- Those capability flags are now runtime-authoritative:
  - disabled RSI-threshold and price-target semantics do not close positions through fallback paths
  - managed recovery target handling also respects `recovery.priceTargetExit`
  - disabled reason strings are sanitized at the coordinator boundary so downstream telemetry/classification does not re-infer disabled semantics
- Paused state is now runtime-authoritative for entry suppression:
  - paused + flat bot: no strategy/entry execution
  - paused + open position: exit handling still runs
  - `RiskManager.canOpenTrade(...)` blocks all opens while paused
- The paused-state dead end found in the final pre-launcher verification is fixed:
  - close outcome shaping now preserves an existing non-empty `pausedReason`
  - runtime state no longer persists `status === "paused"` with a null/empty `pausedReason`
- Documentation-only guardrails were also fixed in code comments:
  - `IndicatorEngine` RSI remains simple-window, not Wilder-smoothed
  - short balance accounting remains an explicit paper-only full-notional simplification, not realistic margin accounting
- As of `2026-04-22`, `npm test` passes after the final paused-state fix and the runtime is a coherent base for launcher/debug work.

## Update - 2026-04-23

- Legacy-backed replay/backtest now hard-fails on unsupported flat-market short-entry semantics instead of silently producing misleading reports:
  - `legacy/backtest.js` aborts when replay sees flat `SELL` / `SELL candidate`
  - `BacktestEngine` wording and README now state explicitly that paper runtime supports shorts but current replay/backtest does not validate short-capable strategies
- Report/export short clarity was tightened without redesigning the reporting stack:
  - `exitLifecycleReport` now matches close events from both `SELL` and `COVER`
  - `exitLifecycleReport` latch analysis now recognizes later `SHORT` entries as valid resumptions alongside `BUY`
  - `ExperimentReporter` now emits additive `sideSummary=long_only|short_only|mixed|none` in line 1 of the text report
  - `SystemServer` short-facing report surfaces are now locked by regression tests for:
    - closed-trade payload `side: "short"`
    - chart markers `SHORT` / `COVER`
    - Pulse / positions payload exposure of open short positions
- Current audit cleanup track is effectively closed for active runtime/report surfaces.
- The main remaining gap is still replay/backtest parity for short-capable strategies.
- Logging cleanup P2 is now complete for v18:
  - `entry_gate_allowed` / `entry_gate_blocked` are the canonical detailed entry decision events
  - Architect append-only payloads now keep causal publish/hold/change facts only
  - `trade_closed` is the canonical detailed exit event
  - `BUY` / `SHORT` / `SELL` / `COVER` / `RISK_CHANGE` remain compact lifecycle transitions
  - `managed_recovery_exited` and `failed_rsi_exit` remain narrow semantic annotations
- Final pre-v19 P1 fixes are now complete:
  - `classifyClosedTrade` prefers structured exit metadata / lifecycle information and keeps reason-string matching only as a bounded fallback for ambiguous RSI subcases
  - architect-side MTF instability gating is configurable through `mtf.instabilityThreshold` with default `0.5`
  - `mtfParamResolver` keeps its separate stricter `0.25` coherence threshold for parameter widening
- The remaining logging work is optional contract formalization for future debug/jsonl export, not another payload-cleanup wave.
- v18 is closed after documentation/release-finalization; no P0 issues remain.

## Update - 2026-04-24

- The experimental Pulse frontend chart is now removed from the active browser path for v18 stability closure.
- `public/index.html` no longer loads `lightweight-charts` or `/ui/chartAdapter.js`.
- `public/pulse.js` no longer fetches `/api/chart`, maintains chart state, or schedules chart refresh intervals.
- Pulse remains the single operator UI at `/`, but its active frontend surface is now limited to:
  - status/system summary
  - bot cards
  - focus panel details
  - recent events
  - history and manual resume actions
- Backend chart support remains intentionally available through `SystemServer.buildChartPayload()` and `GET /api/chart`; this is no longer an active Pulse UI dependency.
- Regression coverage now explicitly locks:
  - no active Pulse dependency on `ChartAdapter`
  - no Pulse request to `/api/chart`
  - continued Pulse focus/events rendering without a chart
- Patch series 1-4B completed the v18 pre-closure stabilization pass:
  - explicit manual recovery for timed-out post-loss Architect latches through `POST /api/bots/:botId/reset-post-loss-latch`
  - market freshness expiry now uses runtime receive/update time instead of exchange tick timestamp
  - TradingBot portfolio kill-switch preview now uses runtime wall-clock for snapshot timestamps
  - degraded/stale data exits remain allowed but emit `degraded_data_exit_warning`
  - `StateStore.registerBot(...)` construction is now explicit about defaults, preserved runtime fields, and config-owned fields
  - `normalizeBotRuntimeState(...)` was renamed to `enforceBotLifecycleStateInvariants(...)`
  - `refreshMarketDataFreshness(...)` was renamed to `markMarketDataStaleIfExpired(...)`
  - MarketStream timing constants are named instead of inline literals
  - generic `// Module responsibility:` source headers were removed
  - AI-agent docs now point all coding agents toward human-readable code rules in `AGENT.md`
- Final v18 release-candidate blockers are closed:
  - post-loss Architect latch is now global at bot level while active; strategy id remains metadata only
  - negative `exitPolicy.recovery.targetOffsetPct` is rejected explicitly
  - default WebSocket construction uses `globalThis.WebSocket` and fails with an actionable Node/native WebSocket requirement if unavailable
- v18 is now release candidate / commit safety, not feature-complete for replay/margin research. The active runtime is paper-only, Pulse-only, and guarded by explicit operator actions for manual resume/latch reset.

## Update - 2026-04-25

- v18.1 is closed after the final audit; no concrete release blockers were found.
- Completed v18.1-A:
  - `ConfigLoader` now rejects bots whose configured base strategy is missing from `allowedStrategies`
  - `TradingBot` catches `strategy.evaluate(...)` failures, logs `strategy_evaluate_failed`, records `strategy_error`, and keeps later ticks alive
- Completed v18.1-B:
  - `POST /api/kill-switch/reset` provides explicit operator reset for triggered portfolio kill-switch state
  - reset clears trigger/blocking fields only and preserves bot pause/cooldown/latch/freshness/balances/trades
- Completed v18.1-C:
  - hold duration and managed recovery timeout use runtime wall-clock
  - `tick.timestamp` remains exchange/event metadata
  - `ContextBuilder` requires finite `observedAt`
  - `WSManager` accepts injected clock for infrastructure timestamps
- Completed v18.1-D:
  - legacy backtest smoke test now asserts deterministic trade/PnL behavior
  - `docs/operations.md` documents latch reset, kill-switch reset, UserStream degraded/disconnected behavior, `paper_full_notional_simplified`, and Node native WebSocket requirements
- Deferred to v18.2:
  - MTF boundary validation through shared helper/schema
  - dependency/layer rules
  - discriminated errors
  - state machines
  - internal config schema helpers
  - boundary contract tests
  - `any` reduction
  - human flow docs

## Planned Next Work

### v18 - Release Candidate / Commit Safety

Goal: release only. Do not start new feature work inside v18.

- Accept only release-critical regressions or documentation fixes.
- Keep paper-only, Pulse-only runtime constraints intact.
- Do not modernize backtest, margin/short realism, or strategy optimization in v18.
- Preserve the final safety fixes:
  - non-throwing event publication around authoritative trading flows
  - global post-loss latch entry block
  - explicit recovery target offset validation
  - native WebSocket runtime guard

### v18.1 - Technical Microfixes

Status: closed.

Goal was targeted microfixes only; no large redesign.

- Tick processing snapshot / hot-path history sharing:
  - reduce duplicate price-history reads
  - introduce one immutable per-tick snapshot only if it can be done without broad churn
  - treat this as high-risk because it spans `MarketStream -> ContextService -> Architect -> TradingBot`
- MTF boundary validation:
  - `configLoader` already validates duplicate frame ids and invalid `windowMs`
  - add a boundary guard in `MtfContextService` only if the validation helper can be shared without duplicating config-loader logic
- Time-base cleanup:
  - remove residual mixed clock usage where it affects deterministic tests or payload consistency
  - do not change trading thresholds or strategy decisions
- Strategy error boundary:
  - isolate strategy evaluation failures only if it can be done locally and observably
  - do not hide errors as successful trading decisions
- Config validation:
  - close small validation gaps with shared helpers
  - avoid duplicate parsing logic across config loader and runtime boundaries
- Portfolio kill-switch reset:
  - add only as an explicit operator action if implemented
  - never auto-reset safety state
- Small deterministic/testability fixes:
  - prefer injected clocks and deterministic fixtures where behavior already exists
- SystemServer clock determinism:
  - replace residual `Date.now()` use in payload/API construction with injected clock plumbing
  - this is for testability/determinism only, not trading behavior
- Legacy backtest smoke test:
  - keep it legacy-adapter only, not modern replay
  - use a deterministic series and assert trade count / expected PnL
  - avoid self-referential `getCapabilities()` style tests
- MarketStream naming cleanup:
  - rename local `handle*` leftovers only where safe and contained
  - avoid cascade renames
- Operational docs/runbook:
  - document post-loss latch reset
  - document what to do when `UserStream` remains disconnected
  - explain `paper_full_notional_simplified`

### v18.2 - Repo Humanization + Boundaries + Contracts + Types

Status: next focus.

Goal: make the repo easier to understand and safer to change before modern replay work begins.

- Key rule: do not segment `TradingBot` before contract tests and clear boundaries exist.
- v18.2-A - Baseline:
  - `any` count
  - circular imports
  - dependency graph
  - boundary violations
- v18.2-B - Boundaries warning mode:
  - dependency-cruiser
  - madge
  - declared temporary exceptions
  - fix the architect/MTF types cycle
  - move `Clock` out of `core`
- v18.2-C - Execution ownership:
  - clarify who mutates position/trade state
  - reduce double ownership between `ExecutionEngine` and `UserStream`
  - add open/close contract tests
- v18.2-D - Error taxonomy v1:
  - `ConfigError`
  - `InvariantError`
  - `ExecutionError`
  - `StrategyError`
  - `MarketDataError`
  - discriminated open/close results
- v18.2-E - Config schema v1:
  - internal schema helpers for bots/runtime/MTF/recovery slices
  - explicit defaults
  - errors with paths
- v18.2-F - State machine selectors:
  - `PositionState`
  - `EntryGuardState`
  - minimal `OrderState`
  - transition/invariant tests
- v18.2-G - Contract test suite:
  - main boundary contracts
  - replace overly fragile tests where useful
- v18.2-H - TradingBot segmentation:
  - only after contracts
  - keep `onMarketTick` as a readable map
  - split entry/exit/architect/tick-prep by clear criteria
- v18.2-I - Any reduction:
  - target at least 70% reduction
  - start with tick-path and boundary events
  - leave legacy/raw payload surfaces as `unknown` plus narrowing
- Exit criteria:
  - baseline is measured and saved
  - dependency-cruiser/madge run in warning mode with declared exceptions
  - zero circular imports between main layers except justified temporary exceptions
  - contract tests are green
  - open/close ownership is clear
  - explicit state machine selectors exist for position/entry guard/order
  - bots/runtime/MTF/recovery config is schema-validated
  - error taxonomy v1 is used in the main paths
  - `any` is reduced by at least 70%
  - entry/exit/recovery/architect flows are clearly documented
  - `TradingBot.ts` is segmented only after contracts and boundaries are clear
- Keep this separate from v19 replay, v20 margin realism, and v21 optimization.

### v19 - Modern Backtest Parity

Goal: build the real test bench. No inflated-profit stories and no lookahead.

- v19-A data layer:
  - choose serious data sources: Binance data.vision, aggTrades/raw trades, klines only where acceptable, and Tardis/Kaiko-class providers if needed
  - add dataset quality scanning for gaps, duplicates, timestamp anomalies, symbol migration, timezone, precision, and exchange filters
- v19-B event-driven replay:
  - target path: `ReplayFeed -> StateStore -> ContextService -> Architect -> TradingBot -> ExecutionEngine`
  - deterministic replay clock
  - no lookahead
  - explicit warm-up
  - MTF without leakage
  - clear candle-close rules
- v19-C execution realism v1:
  - maker/taker fees
  - slippage and spread models
  - exchange filters: min notional, tick size, step size
  - documented market/limit fill assumptions
  - base latency model
  - defer full order book if it would slow delivery too much
- v19-D strategic reporting:
  - total PnL, daily PnL, profitable-day percentage, average/median daily profit, worst day
  - max drawdown, drawdown duration, recovery time, max consecutive losing days
  - profit factor, expectancy, win rate, average win/loss
  - fee/slippage impact
  - regime, strategy, entry reason, and exit reason breakdown
  - managed recovery stats, MTF impact, blocked trades
  - optimize for small consistent gains, not spectacular one-off peaks
- v19-E anti-lookahead harness:
  - test future candle leakage, same-bar bias, unclosed higher timeframe use, false indicator maturity, MTF leakage, and Architect future visibility
- v19-F legacy deprecation path:
  - compare legacy and modern results
  - mark legacy deprecated only after modern replay is validated
  - do not remove legacy until modern replay is trusted

### v20 - Short/Futures/Margin Realism

Goal: make short economics real, not just inverted long PnL.

- Recommended first target: paper futures isolated margin v1.
- Implement unified long/short accounting:
  - `positionSide: long | short`
  - quantity, notional, entry/exit, realized/unrealized PnL
- Add leverage and margin:
  - configurable leverage
  - initial margin, maintenance margin, available margin
  - isolated margin
- Add liquidation model:
  - liquidation price
  - liquidation event
  - forced close
  - optional liquidation fee
- Use mark price vs last price:
  - futures liquidation needs mark price; last price alone is not enough
- Add funding:
  - start simplified
  - use historical funding later if more realism is needed
- Extend portfolio risk:
  - exposure per symbol
  - max notional
  - max leverage
  - portfolio kill-switch compatible with margin

### v21 - Strategy Lab / Optimization

Goal: find robustness, not overfitting.

- Walk-forward analysis:
  - training window
  - validation window
  - rolling windows
- Parameter sweeps:
  - parameter grids
  - sensitivity maps
  - robustness heatmaps
- Out-of-sample validation:
  - strict data separation
  - no tuning on the test set
- Monte Carlo:
  - reshuffle trades
  - bootstrap returns
  - stress negative sequences
- Benchmarks:
  - buy & hold
  - no-trade
  - simple EMA
  - simple RSI
  - controlled random baseline
- Strategy comparison framework:
  - compare strategies on identical data, fee, and slippage assumptions
  - rank by daily stability, drawdown, tail risk, and recovery time, not only profit

## Resolved Earlier Failure

- The earlier `tests/tradingBot.test.js` failure around
  - `max drawdown pause should emit explicit manual-resume-required risk metadata`
  is now resolved.
- Focused verification now passes for:
  - `tests/tradingBot.test.js`
  - `tests/riskManager.test.js`
  - `tests/systemServer.test.js`
  - `tests/orchestrator.test.js`
- Full suite verification after compact monitor and consistency changes: `npm test`.
- Full suite verification after MTF RSI cap-resolution and telemetry diagnostics:
  - `npx -p typescript@5.6.3 tsc -p tsconfig.json --pretty false`
  - `npm test`
  - `git diff --check`

## Recommended Next Order

1. Define the launcher startup contract:
   - first window selects `Normal` vs `Debug`
   - `Debug` opens a second window for selecting run-capture fields
   - keep the launcher/config surface separate from trading decisions
2. Define the debug capture schema before implementation:
   - append-only `jsonl` event families
   - rolling numeric metrics that should be stored as latest values or aggregated counters
   - naming and stability rules for fields that external tooling may consume
3. Extend `BacktestEngine` from adapter-only into a replay feeder that can drive modern decision components without copying legacy runtime logic back into `src/`.
4. If more performance work is needed, stay local to `ContextBuilder`, `ContextService`, and data-shaping utilities. Avoid broad runtime rewrites.
5. If operator tooling is expanded, keep it inside Pulse and on top of the explicit manual-resume API; do not infer resume semantics from generic state changes.
6. Keep exit capability semantics authoritative if launcher/debug work surfaces them:
   - disabled capability flags must not still cause exit or downstream reclassification
7. Preserve paused-state coherence:
   - no persisted `paused + null reason`
   - generic pause reasons remain runtime-authoritative for entry suppression
   - only `max_drawdown_reached` remains resumable through the explicit server API
8. If Architect churn still appears in paper runs, instrument publication count since entry and invalidation grace decisions before changing strategy formulas.
9. If MTF tuning continues, keep changes local to MTF config/aggregation/resolver/economics diagnostics and preserve baseline behavior when `mtf.enabled=false` or `MTF_ENABLED=false`.

## Do Not Do Next

- Do not re-open the live execution path from the active runtime.
- Do not treat `BacktestEngine` adapter coverage as proof of replay parity.
- Do not move architect, latch, or outcome-shaping logic back into `TradingBot`.
- Do not assume replay/backtest short parity exists just because active runtime/report surfaces now have short-aware tests and clear exports.
- Do not reintroduce single-publish `family_mismatch` invalidation for managed recovery.
- Do not reintroduce a second dashboard architecture beside Pulse.
- Do not reintroduce frontend charting into Pulse without a separate rewrite/stability decision.
- Do not let launcher mode selection or debug-output capture leak into runtime trading decisions.
- Do not serialize every tick-sized detail into `jsonl` by default when a rolling numeric snapshot or counter would carry the same signal more cheaply.
- Do not move RSI target-distance rejection into the strategy formula.
- Do not put MTF interpretation or strategy-specific MTF branches into `TradingBot`.
- Do not let raw timeframe labels leak into `mtfParamResolver`.
